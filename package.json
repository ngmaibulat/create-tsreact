{
  "name": "create-tsreact-workspace",
  "version": "0.0.0",
  "description": "Workspace root - the published package is packages/cli",
  "private": true,
  "type": "module",
  "scripts": {
    "format:check": "oxfmt --check packages/bruno/src packages/cli/src packages/png/src packages/pm/src scripts packages/cli/smoke.mjs",
    "format:fix": "oxfmt packages/bruno/src packages/cli/src packages/png/src packages/pm/src scripts packages/cli/smoke.mjs",
    "lint": "oxlint",
    "typecheck": "tsc --noEmit -p packages/bruno && tsc --noEmit -p packages/pm && tsc --noEmit -p packages/png && tsc --noEmit -p packages/cli",
    "build": "pnpm --filter create-tsreact run build",
    "build-watch": "pnpm --filter create-tsreact run build-watch",
    "test": "pnpm --filter create-tsreact run test",
    "release": "node scripts/release.mjs",
    "prepare": "husky"
  },
  "license": "MIT",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "oxfmt": "^0.62.0",
    "oxlint": "^1.70.0",
    "typescript": "^7.0.2"
  }
}
